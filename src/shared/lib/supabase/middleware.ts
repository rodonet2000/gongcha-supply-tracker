// This file is kept for reference but the main middleware.ts now uses
// a simpler cookie-presence check instead of initializing a full Supabase client.
// Session validation is handled by Server Components via getCurrentUser().
export {}
